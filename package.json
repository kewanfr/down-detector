{
  "name": "down-detector",
  "version": "1.5.5",
  "description": "Simple utility to detect if my network is down or not",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update": "git pull && npm install && pm2 restart DOWN && pm2 logs DOWN"
  },
  "type": "module",
  "author": "kewanfr",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^14.17.3",
    "dotenv": "^16.4.7",
    "nodemailer": "^6.10.0",
    "ping": "^0.4.4",
    "proxmox-api": "^1.1.1"
  }
}
